739. Daily Temperatures



//ai


//start
int[] temperatures = {73, 74, 75, 71, 69, 72, 76, 73};
int n = temperatures.length;
int[] answer = new int[n]; //all zeros by default
Deque<Integer> stack = new ArrayDeque<>(); // 인덱스를 저장 (감소하는 온도 순서 유지)

		for (int i = 0; i < n; i++) {
			// 현재 온도가 스택 top의 온도보다 높으면 -> 그 날의 답 확정
			while (!stack.isEmpty() && temperatures[i] > temperatures[stack.peek()]) {
				int prevIndex = stack.pop();
				answer[prevIndex] = i - prevIndex;
			}
			stack.push(i);
		}

		System.out.println(Arrays.toString(answer));
		//end
		//Output: [1,1,4,2,1,1,0,0]



my answer
	int[] temperatures = {73, 74, 75, 71, 69, 72, 76, 73};

		//start
		ArrayList<Integer> result = new ArrayList<>();
		int temperaturesLength = temperatures.length - 1; // for array
		for (int i = 0; i < temperaturesLength; i++) {

			for (int j = i + 1; j <= temperaturesLength; j++) {

				if (temperatures[i] < temperatures[j]) {
					result.add(j - i);
					break;
				}

				if (temperatures[i] >= temperatures[j]&& j== temperaturesLength) {
					result.add(0);
					break;
				}
			}
		}
		result.add(0);
		System.out.println(result);