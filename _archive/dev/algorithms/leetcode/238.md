
238. Product of Array Except Self

Given an integer array nums, return an array answer such that answer[i] is equal to the product of all the elements of nums except nums[i].

The product of any prefix or suffix of nums is guaranteed to fit in a 32-bit integer.

You must write an algorithm that runs in O(n) time and without using the division operation.

Example 1:

Input: nums = [1,2,3,4]
Output: [24,12,8,6]
Example 2:

Input: nums = [-1,1,0,-3,3]
Output: [0,0,9,0,0]


AI풀이

```java
class Solution {
    public int[] productExceptSelf(int[] nums) {
        int n = nums.length;
        int[] result = new int[n];
        
        // 1단계: result[i] = i 왼쪽에 있는 모든 원소의 곱 (prefix product)
        result[0] = 1;  // 첫 원소 왼쪽엔 아무것도 없으니 항등원 1
        for (int i = 1; i < n; i++) {
            result[i] = result[i - 1] * nums[i - 1];
        }
        
        내가 온몸비틀기로 맞추던 방식이 아닌 이런식으로 애초에 1을 넣고 구함 마지막값 필요없으니까 
                

        // 2단계: 오른쪽에서 왼쪽으로 훑으며 suffix 곱을 곱해줌
        int suffix = 1;  // 마지막 원소 오른쪽엔 아무것도 없으니 1

        for (int i = n - 1; i >= 0; i--) {
            result[i] *= suffix;
            suffix *= nums[i];
        }

        return result;
    }
}
```

내풀이
```java
 package com.example.java_coding_test;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

import java.util.Arrays;
import java.util.List;

@SpringBootApplication
public class JavaCodingTestApplication {

    public static void main(String[] args) {
        SpringApplication.run(JavaCodingTestApplication.class, args);

        int[] nums = {1, 2, 3, 4};
        int len = nums.length;
        int[] prefixArr = new int[len];
        int[] suffixArr = new int[len];
        int[] result = new int[len];
        int prefix = 1;
        int suffix = 1;


        for (int i = 0; i < len; i++) {
            prefix *=nums[i];
            prefixArr[i] =prefix;

            suffix  *= nums[len-i-1];
            suffixArr[len-i-1] =suffix;
        }

        for(int j =1;  j<=len;j++ ){
            if(j==1){
                result[j-1] = suffixArr[j];
            }else if(j == len){
                result[j-1] = prefixArr[j-2];
            }else{
                result[j-1] = suffixArr[j]* prefixArr[j-2];
            }

        }
        System.out.println(Arrays.toString(result));
    }
}

 

```