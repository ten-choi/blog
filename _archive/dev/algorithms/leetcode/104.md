

15 3sum

```java

 public class TreeNode {
      int val;
      TreeNode left;
      TreeNode right;
      TreeNode() {}
      TreeNode(int val) { this.val = val; }
      TreeNode(int val, TreeNode left, TreeNode right) {
          this.val = val;
          this.left = left;
          this.right = right;
      }
  }
 
class Solution {
    public int maxDepth(TreeNode root) {
        if (root == null) {return 0;}
        if(root.left!= null)maxDepth(root.left, i);
        if(root.right!= null)maxDepth(root.right, i);
        
        
        int j = 0 ; 
        int k=0;
        int j=0;
        recursive(root, k);
        return j;
    }
    public int recursive(TreeNode root, int i){
        i+=1;
        if(root.left!= null)recursive(root.left, i);
        if(root.right!= null)recursive(root.right, i);
        if(j<i)j=i;
        i-=1;
    }

}


```